import { ChatMessageWrapper } from './chat-message-wrapper';

describe('ChatMessageWrapper', () => {
  it('should create an instance', () => {
    expect(new ChatMessageWrapper()).toBeTruthy();
  });
});

import { InvestigationReport } from './investigation-report';

describe('InvestigationReport', () => {
  it('should create an instance', () => {
    expect(new InvestigationReport()).toBeTruthy();
  });
});

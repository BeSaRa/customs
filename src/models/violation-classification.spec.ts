import { ViolationClassification } from './violation-classification';

describe('ViolationClassification', () => {
  it('should create an instance', () => {
    expect(new ViolationClassification()).toBeTruthy();
  });
});

import { CollaborationDetails } from './collaboration-details';

describe('CollaborationDetails', () => {
  it('should create an instance', () => {
    expect(new CollaborationDetails()).toBeTruthy();
  });
});

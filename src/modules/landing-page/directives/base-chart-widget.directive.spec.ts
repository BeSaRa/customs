import { BaseChartWidgetDirective } from './base-chart-widget.directive';

describe('BaseChartWidgetDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseChartWidgetDirective();
    expect(directive).toBeTruthy();
  });
});

import { PenaltyDecisionInterceptor } from './penalty-decision-interceptor';

describe('PenaltyDecisionInterceptor', () => {
  it('should create an instance', () => {
    expect(new PenaltyDecisionInterceptor()).toBeTruthy();
  });
});

import { GeneralInterceptor } from './general-interceptor';

describe('GeneralInterceptor', () => {
  it('should create an instance', () => {
    expect(new GeneralInterceptor()).toBeTruthy();
  });
});

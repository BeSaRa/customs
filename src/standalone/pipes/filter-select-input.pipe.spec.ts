import { FilterSelectInputPipe } from './filter-select-input.pipe';

describe('FilterSelectInputPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSelectInputPipe();
    expect(pipe).toBeTruthy();
  });
});

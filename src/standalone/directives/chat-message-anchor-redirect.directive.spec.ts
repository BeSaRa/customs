import { ChatMessageAnchorRedirectDirective } from './chat-message-anchor-redirect.directive';

describe('ChatMessageAnchorRedirectDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatMessageAnchorRedirectDirective();
    expect(directive).toBeTruthy();
  });
});
